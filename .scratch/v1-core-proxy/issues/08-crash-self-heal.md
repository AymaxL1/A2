# 08 — 崩溃自愈

**What to build:** 崩溃/强杀后用户不断网:宿主下次启动时检测系统代理是否仍指向本应用的已死端口,是则自动二选一——恢复接管(拉起内核后重设)或还原快照——绝不滞留「代理指向死端口」的断网态。判定逻辑(何时恢复、何时还原)为纯逻辑可测。

**Blocked by:** 07

**Status:** done(`b982aa5`;审后修复 `2efe51f`/`854cb63`/`38b5eac`/`bf5ab58`:非法接管态 fail-safe、恢复时保留本地代理、tombstone 清态)

**验证环:** vfsoverlay(今天可验,E2E 用 kill -9 剧本)。

- [ ] 强杀宿主后重启:检测到残留接管 → 自动恢复或还原,系统代理不再指向死端口
- [ ] 判定逻辑假件测试覆盖:残留接管/无残留/用户手动改过代理等分支
- [ ] E2E 剧本:接管 → kill -9 → 重启 → 断言系统代理终态与内核状态
- [ ] check.sh 全绿
