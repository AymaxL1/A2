# 10 — 订阅管理(normal 更新 + dangerous 换源)

**What to build:** 订阅生命周期全链:可存多个订阅、同一时刻只激活一个、手动触发更新已有源(`proxy.subscription.update`,normal 零打断);新增/替换订阅源为 dangerous——宿主 GUI 弹最终确认,批准才写入,拒绝不留痕。这是平台 dangerous 模型的第一个真实业务用例(04 票机制的首次实战),也是旗舰场景的「必确认」半边。

**Blocked by:** 04, 06

**Status:** done(`3368d3b`,PASS=337)

**验证环:** vfsoverlay(今天可验;订阅源可用本地文件/本地 HTTP 假源)。

- [ ] 多订阅存储、单一激活、切换激活后内核配置随之生效
- [ ] 更新已有源:normal 零确认;新增/替换源:必经宿主确认,批准/拒绝两分支行为正确
- [ ] 经 CLI 与经 UDS 直连均无法绕过换源确认(复用 04 票反向用例)
- [ ] 订阅状态机(存储/激活/更新失败回滚)纯逻辑假件测试覆盖
- [ ] check.sh 全绿
